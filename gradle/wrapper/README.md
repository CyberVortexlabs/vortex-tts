# Gradle bootstrap wrapper

این پروژه از Gradle 8.7 استفاده می‌کند. فایل `gradlew` در ریشه پروژه در صورت نبود توزیع محلی، Gradle 8.7 را از منبع رسمی Gradle دریافت و اجرا می‌کند؛ بنابراین فایل باینری wrapper در این artifact قرار نگرفته است.
